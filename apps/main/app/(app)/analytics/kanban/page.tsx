import React from "react";
import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Kanban | Ascentio"
};


export default async function KanbanPage() {
    return <div className=" w-full"></div>;
}