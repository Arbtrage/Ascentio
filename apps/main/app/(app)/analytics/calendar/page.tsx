import React from "react";
import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Calendar | Ascentio"
};


export default async function CalendarPage() {
    return <div className=" w-full"></div>;
}