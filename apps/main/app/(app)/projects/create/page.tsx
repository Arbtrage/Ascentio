import React from "react";
import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Create Project | Ascentio"
};


export default async function CreateProjectPage() {
    return <div className=" w-full"></div>;
}